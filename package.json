{
  "name": "foobar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:node": "esbuild server.tsx --platform=node --bundle --outdir=server --format=cjs --define:SERVER=true",
    "build:client": "esbuild client.tsx --bundle --minify --splitting --sourcemap --outdir=build --format=esm --define:SERVER=false",
    "build": "concurrently \"npm run build:node\" \"npm run build:client\"",
    "start": "node ./server/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react-dom": "^17.0.11",
    "esbuild": "^0.14.10",
    "express": "^4.17.2",
    "lodash": "^4.17.21",
    "reac": "0.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}